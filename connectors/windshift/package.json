{
  "name": "@getomnico/windshift-connector",
  "version": "1.0.0",
  "description": "Windshift connector for Omni",
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && NODE_ENV=production node --test dist/*.test.js",
    "start": "node dist/main.js",
    "dev": "tsc --watch",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {
    "@getomnico/connector": "file:../../sdk/typescript",
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "license": "Apache-2.0"
}
